package application;

public class SampleController {
	
	
	

/*if(algún campo no cuadra y falla){
Alert alerta=new Alert(AlertType.tipoDeAlerta);
alert.setTitle("Titulo del borde de la alerta");
alerta.setHeaderText("Explicacion breve, que será el texto cabecilla");
alerta.setContentText("Contenido de la alerta, explicacion un poco más extensa");
alerta.showAndWait();

}else{
	tablaLibros.getItems().remove(num);
	tableLibros.getSelectionModel().clearSelection();
}


public void borrarLibro(ActionEvent ev){
	int indice=variableTabla.getSelectionModel().getSelectedIndex();
	variableTabla.getItems().remove(indice);
}*/
	
}
